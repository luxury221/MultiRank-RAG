# Outputs

This directory is used for generated runtime artifacts:

- parsed nodes and edges
- visual evidence files
- embedding caches
- retrieval and reranking CSV files
- evidence chains and cards
- benchmark outputs
- server logs

The directory is intentionally ignored by Git. Keep important findings in `docs/` instead of committing generated artifacts.

